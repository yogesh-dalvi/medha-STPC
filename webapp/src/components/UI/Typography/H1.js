import React from "react";
import { Typography } from "@material-ui/core";
const H1 = props => {
  return (
    <Typography>
      <h1>{props.elemetData}</h1>
    </Typography>
  );
};

export default H1;
